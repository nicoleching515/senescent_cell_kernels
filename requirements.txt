# SASP Kernel project — pinned 2026-08-20, Python 3.11
#
# 2026-08-21: the working container was reset and the entire scientific stack was
# lost (only the base numpy 1.26.3 survived). Everything below the "core" block
# was reinstalled from these pins and the environment-drift question the README
# raised was then settled empirically — see "Reproducibility check", below.

# --- core: reinstalled and verified 2026-08-21 -------------------------------
numpy==2.4.6
scipy==1.17.1
pandas==2.3.3
pyarrow==25.0.1
h5py==3.16.0
scikit-learn==1.9.0
statsmodels==0.14.6
matplotlib==3.11.1
seaborn==0.13.2
joblib==1.5.3
tqdm==4.70.0

# --- published CCC software run as released (Phase 4 / Figure 4) -------------
# commot 0.0.3 CANNOT BE IMPORTED under numpy>=2: it has `rho = np.Inf` as a
# default argument in commot/_optimal_transport/_usot.py, and np.Inf was removed
# in NumPy 2.0. code/_shims/np2_compat.py restores the removed aliases (they are
# pure aliases, so nothing numerical changes) and phase4_run.py imports it before
# commot. This means the ORIGINAL Phase 4 COMMOT results were produced under
# numpy 1.x; see the reproducibility check below for why that is now settled.
commot==0.0.3
anndata==0.12.19

# --- sender-calling comparison only (BIO_PHASE3 §4) --------------------------
# Not required to reproduce any figure or headline number; needed only to re-run
# the caller-disagreement analysis. DeepScence is run with denoise=False because
# its DCA dependency needs an obsolete TensorFlow stack (see code/_shims/dca).
scanpy==1.11.5
pointpats==2.5.5
leidenalg==0.12.0
igraph==1.0.0
DeepScence==1.0.0
senepy==1.0.1
# formulaic: not installed

# --- Reproducibility check, 2026-08-21 ---------------------------------------
# The README previously flagged, honestly, that numpy drifted 1.26.3 -> 2.4.6
# mid-project and that this had "not been re-verified end to end". It now has
# been, at two points:
#
#   1. Phase 4 / COMMOT. A stored checkpoint (commot_7259_t0_real_p0, written
#      under numpy 1.26.3) was recomputed under numpy 2.4.6 + the alias shim:
#      81/81 rows, all p-values bit-identical, max |diff| on the score column
#      3.4e-21. See CS_PHASE4 §8.
#   2. Phase 1 / the synthetic sweep. Re-run in full under numpy 2.4.6 and
#      diffed against the stored results — see reports/CS_PHASE1.md and
#      results/repro_2026-08-21/.
